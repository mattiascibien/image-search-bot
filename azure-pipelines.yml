trigger:
- master

stages:
- stage: build
  jobs:
    - template: .azure/build-template.yml  # Template reference
      parameters:
        name: dotnet_cli
        vmImage: 'ubuntu-latest'
        artifact: dotnet_cli
